{
  "version": "0.2.0",
  "configurations": [
      {
          // for MacOS
          "name": "(lldb) Launch exe",
          "type": "cppdbg",
          "request": "launch",
          "program": "${workspaceRoot}/build/main.exe",
          "args": [],
          "stopAtEntry": false,
          "cwd": "${workspaceRoot}",
          "environment": [],
          "externalConsole": true,
          "preLaunchTask": "make",
          "MIMode": "lldb"
      }
  ]
}